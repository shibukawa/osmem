---
id: doc:management-api
type: doc
title: Management API Page
---
Reference for the /_osmem endpoints and the frozen-base rule, for people writing their own helper in another language.

```yaml
summary:
  sections:
    - endpoint table (info, base, freeze/unfreeze, clones CRUD)
    - ready line and process contract of osmem-server (stdin EOF, --parent-pid, signals)
    - which requests a frozen base still accepts
    - example session with curl
  references:
    - api:clone-admin-api
    - api:server-cli
```
